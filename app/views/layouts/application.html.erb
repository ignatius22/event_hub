<!DOCTYPE html>
<html class="h-full bg-warm-50">
  <head>
    <title>EventHub - Community Event Management</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <!-- Navigation -->
    <nav class="bg-white shadow-soft border-b border-warm-100">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <%= link_to root_path, class: "text-2xl font-bold bg-gradient-to-r from-primary-600 to-primary-500 bg-clip-text text-transparent" do %>
                EventHub
              <% end %>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
              <%= link_to "Events", events_path, class: "border-transparent text-warm-600 hover:border-primary-300 hover:text-primary-600 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
              <% if user_signed_in? && current_user.can_create_events? %>
                <%= link_to "My Events", my_events_path, class: "border-transparent text-warm-600 hover:border-primary-300 hover:text-primary-600 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
              <% end %>
              <% if user_signed_in? && current_user.admin? %>
                <%= link_to "Admin", admin_dashboard_path, class: "border-transparent text-warm-600 hover:border-primary-300 hover:text-primary-600 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
              <% end %>
            </div>
          </div>
          <div class="hidden sm:ml-6 sm:flex sm:items-center">
            <% if user_signed_in? %>
              <div class="ml-3 relative" data-controller="dropdown">
                <div>
                  <button type="button" data-action="click->dropdown#toggle" class="bg-white rounded-full flex text-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500" id="user-menu-button">
                    <span class="sr-only">Open user menu</span>
                    <div class="h-8 w-8 rounded-full bg-gradient-to-br from-primary-500 to-primary-600 flex items-center justify-center text-white font-medium shadow-sm">
                      <%= current_user.first_name[0].upcase %><%= current_user.last_name[0].upcase %>
                    </div>
                  </button>
                </div>
                <div data-dropdown-target="menu" class="hidden origin-top-right absolute right-0 mt-2 w-48 rounded-lg shadow-soft-lg py-1 bg-white ring-1 ring-warm-200 focus:outline-none z-50">
                  <div class="px-4 py-2 text-xs text-warm-500">
                    Signed in as<br>
                    <span class="font-medium text-warm-900"><%= current_user.email %></span>
                  </div>
                  <hr class="my-1 border-warm-100">
                  <%= link_to "Your Profile", edit_user_registration_path, class: "block px-4 py-2 text-sm text-warm-700 hover:bg-primary-50 hover:text-primary-700" %>
                  <% if current_user.can_create_events? %>
                    <%= link_to "Create Event", new_event_path, class: "block px-4 py-2 text-sm text-warm-700 hover:bg-primary-50 hover:text-primary-700" %>
                  <% end %>
                  <%= link_to "My Registrations", my_registrations_path, class: "block px-4 py-2 text-sm text-warm-700 hover:bg-primary-50 hover:text-primary-700" %>
                  <hr class="my-1 border-warm-100">
                  <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "block w-full text-left px-4 py-2 text-sm text-warm-700 hover:bg-accent-50 hover:text-accent-600" %>
                </div>
              </div>
            <% else %>
              <div class="flex items-center space-x-4">
                <%= link_to "Sign in", new_user_session_path, class: "text-warm-600 hover:text-primary-600 text-sm font-medium" %>
                <%= link_to "Sign up", new_user_registration_path, class: "bg-gradient-to-r from-primary-500 to-primary-600 hover:from-primary-600 hover:to-primary-700 text-white px-4 py-2 rounded-lg text-sm font-medium shadow-sm hover:shadow-md" %>
              </div>
            <% end %>
          </div>
          <!-- Mobile menu button -->
          <div class="-mr-2 flex items-center sm:hidden">
            <button type="button" data-controller="mobile-menu" data-action="click->mobile-menu#toggle" class="inline-flex items-center justify-center p-2 rounded-md text-warm-400 hover:text-warm-500 hover:bg-warm-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-primary-500">
              <span class="sr-only">Open main menu</span>
              <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
              </svg>
            </button>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice.present? || alert.present? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <% if notice.present? %>
          <div class="rounded-md bg-green-50 p-4 mb-4">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                </svg>
              </div>
              <div class="ml-3">
                <p class="text-sm font-medium text-green-800"><%= notice %></p>
              </div>
            </div>
          </div>
        <% end %>
        <% if alert.present? %>
          <div class="rounded-md bg-red-50 p-4 mb-4">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                </svg>
              </div>
              <div class="ml-3">
                <p class="text-sm font-medium text-red-800"><%= alert %></p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- Main Content -->
    <main>
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-warm-200 mt-auto">
      <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
        <p class="text-center text-sm text-warm-500">
          &copy; <%= Time.current.year %> <span class="font-medium text-primary-600">EventHub</span>. Built with Ruby on Rails.
        </p>
      </div>
    </footer>
  </body>
</html>
